# Compiled binary (built via `go build -o ./straddle-pp-cli ./cmd/straddle-pp-cli`)
/straddle-pp-cli

# Build / release artifacts
/build/
/dist/

# Local SQLite store + cache
*.db
*.db-shm
*.db-wal
demo/out/
